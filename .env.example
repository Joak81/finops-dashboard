# Azure Authentication
# Opção 1: Service Principal (Recomendado para produção)
AZURE_CLIENT_ID=your-service-principal-client-id
AZURE_CLIENT_SECRET=your-service-principal-client-secret
AZURE_TENANT_ID=your-azure-tenant-id

# Opção 2: Usar Azure CLI (para desenvolvimento local)
# Execute: az login
# Não precisa das variáveis acima se usar Azure CLI

# Azure Subscriptions (separadas por vírgula)
AZURE_SUBSCRIPTIONS=subscription-id-1,subscription-id-2,subscription-id-3

# Ou use os IDs baseados no CSV
# AZURE_SUBSCRIPTIONS=DDD-DV,DSP-Infra-PR,CAS-CALAB-DV

# Database Configuration
DB_SERVER=finops-dashboard-sql.database.windows.net
DB_USER=finopsadmin
DB_PASSWORD=FinOps2024!
DB_NAME=finops-dashboard-db

# Application Configuration
PORT=3000
NODE_ENV=development

# Sync Configuration
AZURE_SYNC_INTERVAL_MINUTES=15
ENABLE_AUTO_SYNC=true